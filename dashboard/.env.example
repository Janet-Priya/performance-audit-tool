# Create .env in this folder (Create React App loads it at build/dev time).
# `npm start`: leave unset — the app calls http://127.0.0.1:8001 directly (CORS on the manager).
# `npm run build` + static file server: set the public manager URL, e.g.:
# REACT_APP_API_URL=http://127.0.0.1:8001
